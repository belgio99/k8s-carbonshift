apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carbonshift-umbrella.fullname" . }}-test-connection"
  labels:
    {{- include "carbonshift-umbrella.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carbonshift-umbrella.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
