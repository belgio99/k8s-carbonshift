apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carbonshift-operator.fullname" . }}-test-connection"
  labels:
    {{- include "carbonshift-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carbonshift-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
